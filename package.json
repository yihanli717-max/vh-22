{
  "name": "biztrack",
  "version": "1.0.0",
  "private": true,
  "description": "BizTrack static frontend with Vitest coverage for coursework evidence.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "jsdom": "^26.1.0",
    "vitest": "^3.2.4"
  }
}
